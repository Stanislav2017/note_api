let chai = require('chai');
let chaiHttp = require('chai-http');
let server = require('../app');

chai.should();

chai.use(chaiHttp);

describe('Note Api', () => {

    describe('Test Note Api GET /api/note/list', () => {
    
    });

    describe('Test Note Api GET /api/note/create', () => {

    });

    describe('Test Note Api GET /api/note/update', () => {

    });

    describe('Test Note Api GET /api/note/delete', () => {

    });

    describe('Test Note Api GET /api/note/public-link', () => {

    });
});